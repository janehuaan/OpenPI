node_modules/
dist/
release/
*.log
.DS_Store

# Electron runtime bundle is a build product - never commit it (the old repo
# tracked runtime/package.json and it accumulated dead file:/var/folders paths).
runtime/
